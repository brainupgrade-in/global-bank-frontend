import { Account } from "./account";

export class Customer {
    accounts?: [Account];
    address: string;
    dateOfBirth: string;
    pan:string;
    password:string;
    userid:string;
    username:string;
}